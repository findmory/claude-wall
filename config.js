// Animation speed configuration.
// baseMinDuration / baseMaxDuration define the random fall-duration range
// (in seconds) at normal speed. A shorter duration means a faster fall.
// `speed` scales the fall rate: >1 = faster, <1 = slower, 1 = default.
const config = {
  baseMinDuration: 8, // seconds (current behavior)
  baseMaxDuration: 20, // seconds (current behavior)
  speed: 1, // multiplier: >1 = faster, <1 = slower
};
